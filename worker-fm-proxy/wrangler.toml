name = "ninetone-fm-image-proxy"
# Pinned to micke.ohlen@gmail.com's account (same reason as the site Worker; see wrangler.jsonc there).
account_id = "0d392e5c79e386966a98a214ac91a133"
main = "src/index.ts"
compatibility_date = "2025-09-01"

# Set via: wrangler secret put FM_USER / FM_PASS
# Plain vars below are non-secret config.

[vars]
FM_HOST = "files.ninetone.com"
FM_DB = "Ninetone Group AB"
